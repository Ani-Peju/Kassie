import React from "react";

const Header = () => {
    return (
        <header>
            <img src="kassie.png" alt="Logo" className="items-left" />
        </header>
    );
};

export default Header;